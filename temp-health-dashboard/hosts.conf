#!/usr/bin/env bash

# Map dashboard host names to one or more IPs. Every configured test runs once per IP.
declare -Ag HOST_IPS=(
  [localhost]="127.0.0.1,::1"
)

# Real remote systemd units checked with: systemctl is-active <unit>
declare -Ag HOST_SERVICES=(
  [localhost]="ssh,docker"
)

# Real remote Docker containers. Leave a host unset/empty to discover containers with docker ps on every IP.
declare -Ag HOST_CONTAINERS=(
  [localhost]=""
)

# SSH_USER="ops"
# SSH_OPTS="-o BatchMode=yes -o ConnectTimeout=5 -o StrictHostKeyChecking=accept-new"
# DOCKER_LOG_LINES=40
