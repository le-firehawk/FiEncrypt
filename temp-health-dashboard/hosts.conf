#!/usr/bin/env bash

declare -Ag HOST_IPS=(
  [edge-a]="127.0.0.1,192.0.2.10"
  [app-b]="127.0.0.1"
)

declare -Ag HOST_SERVICES=(
  [edge-a]="ssh,nginx,cron"
  [app-b]="ssh,fi-encrypt-worker"
)

declare -Ag HOST_CONTAINERS=(
  [edge-a]="gateway,metrics"
  [app-b]="api,worker,postgres"
)
